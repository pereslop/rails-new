- if current_user.follows?(user)
  = link_to 'unfollow', unfollow_account_user_path(user), id: 'follow', class: 'btn btn-danger btn-sm', method: :get, remote: true
- else
  = link_to 'follow', follow_account_user_path(user), id: 'follow', class: 'btn btn-primary btn-sm', method: :get, remote: true
