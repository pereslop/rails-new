= simple_form_for [:account, message.conversation, message], remote: true do |f|
  = f.input :body, label: false
  = f.hidden_field :conversation_id, value: message.conversation.id
  = f.button :submit, 'SEND', class: 'send-message no-padding'
