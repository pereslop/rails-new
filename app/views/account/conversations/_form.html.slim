= simple_form_for [:account, conversation, message_body], remote: true do |f|
  = f.input :body, label: false
  = f.hidden_field :conversation_id, value: conversation.id
  = f.button :submit, 'SEND', class: 'send-message no-padding'
